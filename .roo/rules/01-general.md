# General rules

1. The backend is FastAPI-based
2. Always assume packages are already installed
3. User will be the verifier so you don't need to verify the result.
4. The planning/todo of the task should be as detail as possible to increase project understand.